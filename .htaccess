<IfModule mod_rewrite.c>
    RewriteEngine On

    # Serve existing files/directories as is
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    # Everything else -> Laravel public folder
    RewriteRule ^ public/$1 [L]
</IfModule>